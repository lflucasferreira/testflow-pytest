#!/usr/bin/env bash
set -euo pipefail

source "$(dirname "$0")/lib/author-policy.sh"

msg_file="$1"
tmp="$(mktemp)"
sed '/^[Cc]o-[Aa]uthored-[Bb]y:/Id' "$msg_file" > "$tmp"
mv "$tmp" "$msg_file"

author_policy_check_message_file "$msg_file"
